{
  "name": "solid-waste-laboratory",
  "private": true,
  "scripts": {
    "data:build": "node scripts/build-catalog.mjs",
    "data:validate": "node scripts/validate-catalog.mjs",
    "pages:build": "node scripts/build-pages.mjs",
    "check": "npm run data:validate && git diff --check"
  }
}
